{
  "name": "devotion-root",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev:all --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend"
  },
  "dependencies": {
    "axios": "^1.15.0",
    "hls.js": "^1.6.15",
    "lucide-react": "^1.8.0",
    "react-router-dom": "^7.14.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
